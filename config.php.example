<?php
/**
 * Конфигурация приложения
 * 
 * ВАЖНО: Скопируйте этот файл в config.php и отредактируйте с вашими данными!
 * НЕ коммитьте config.php в git (он в .gitignore)
 */

// ===== БАЗА ДАННЫХ =====
define('DB_HOST', 'localhost');      // Хост БД
define('DB_USER', 'root');           // Пользователь БД
define('DB_PASS', 'password');       // Пароль БД
define('DB_NAME', 'clinic_db');      // Имя БД
define('DB_PORT', 3306);             // Порт (обычно 3306)

// ===== ПРИЛОЖЕНИЕ =====
define('APP_NAME', 'Clinic Schedule System');
define('APP_VERSION', '1.0.0');
define('APP_URL', 'http://localhost:8000');

// ===== БЕЗОПАСНОСТЬ =====
define('JWT_SECRET', 'your_super_secret_key_change_this_in_production');
define('JWT_ALGORITHM', 'HS256');
define('TOKEN_EXPIRY', 86400);  // 24 часа в секундах

// ===== РЕЗЕРВНОЕ КОПИРОВАНИЕ =====
define('BACKUP_DIR', __DIR__ . '/backups/');           // Папка для резервных копий
define('BACKUP_AUTO', true);                           // Автоматическое копирование
define('BACKUP_TIME', '23:00');                        // Время автокопирования
define('BACKUP_RETENTION_DAYS', 30);                   // Хранить копии N дней

// ===== ЛОГИРОВАНИЕ =====
define('LOG_DIR', __DIR__ . '/logs/');
define('DEBUG_MODE', true);                            // true для разработки, false для продакшена

// ===== CORS =====
define('ALLOWED_ORIGINS', array(
    'http://localhost:8000',
    'http://localhost:3000',
    'http://127.0.0.1:8000'
));

// ===== EMAIL (для уведомлений) =====
define('MAIL_FROM', 'noreply@clinic.local');
define('MAIL_SMTP_HOST', 'localhost');
define('MAIL_SMTP_PORT', 587);
define('MAIL_SMTP_USER', '');
define('MAIL_SMTP_PASS', '');

// Таймауты
define('DB_TIMEOUT', 30);     // Секунды
define('API_TIMEOUT', 10);    // Секунды

// ===== ПУТИ =====
define('BASE_PATH', __DIR__);
define('BACKEND_PATH', BASE_PATH . '/backend');
define('FRONTEND_PATH', BASE_PATH . '/frontend');
